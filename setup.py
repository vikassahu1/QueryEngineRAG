from setuptools import find_packages, setup

setup(
    name = 'QApplication',
    version= '0.0.1',
    author= 'vikas kumar sahu',
    author_email= '2003vikas0906@gmail.com',
    packages= find_packages(),
    install_requires = []
)


# setup.py is used to install all local packages.
#  It is used to install all packages in the current directory.
# the directory should contain __init__.py

